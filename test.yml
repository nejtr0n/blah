RequestHeaders:
  type: object
  required:
    - version
    - requester
    - timestamp
  properties:
    token:
      type: string
      description: Jasd
    ip:
      type: string
      description: asd
    version:
      type: string
      description: asd
    requester:
      type: string
      description: asd
    timestamp:
      type: integer
      description: asd
